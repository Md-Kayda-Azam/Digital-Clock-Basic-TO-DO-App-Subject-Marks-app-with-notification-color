/**
 * // gettimg dynamic count value
 * @param {*} start_val 
 * @param {*} current_val 
 * @returns 
 */
 function loader_dynamic(start_val, current_val) {
    return (current_val * 100) / start_val;
  };
  